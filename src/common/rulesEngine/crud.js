 /**
* Data Item Module for Omniboard
*/
angular.module("omniboard.rulesEngine.crud", [])

/**
* CRUD Wrappers
*/
.factory('rulesData', ['$firebase', 'es', '$q', '$rootScope', '$filter', 'FirebaseRootRef', function($firebase, es, $q, $rootScope, $filter, FirebaseRootRef) {
	return {

	};
}])

;
