/**
* Data Item Module for Omniboard
*/
angular.module("omniboard.Reporting.crud", [])

/**
* CRUD Wrappers
*/
.factory('ReportData', ['$firebase', 'es', '$q', '$rootScope', '$filter', 'FirebaseRootRef', 'DataModels',  function($firebase, es, $q, $rootScope, $filter, FirebaseRootRef, DataModels) {
	return {
		
	};
}])

;
