 /**
* Data Item Module for Omniboard
*/
angular.module("omniboard.activeLoadTrend.crud", [])

/**
* CRUD Wrappers
*/
.factory('activeLoadTrendData', ['$firebase', 'es', '$q', '$rootScope', '$filter', 'FirebaseRootRef', function($firebase, es, $q, $rootScope, $filter, FirebaseRootRef) {
	return {

	};
}])

;
